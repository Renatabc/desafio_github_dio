var y = 0;
var x = 1;

var z = 2;

// variável é o valor declarado no código

var preco = 2;
var deconto = 0.2;
const preco = 2;
var total = preco - desconto;

//const não pode ser um valor manipulado

function soma(a, b) {
    return a + b;
}

soma(3, 5);
//parêntese declara atributos

// return para o caso de voltar a manipular o valor

//console.log caso não queira retornar o valor